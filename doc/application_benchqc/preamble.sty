% SORT OF ENVIRONMENT VARIABLES
%--------------------------------------

\newif\ifenglish

\englishtrue

% folder
%--------------------------------------
\RequirePackage{import}
\RequirePackage{booktabs}
%--------------------------------------

% Language
%--------------------------------------
\RequirePackage[ngerman]{babel}
%\usepackage[english]{babel}
\RequirePackage[parfill]{parskip}

% Encoding
%--------------------------------------
\RequirePackage[utf8]{inputenc}
\RequirePackage[T1]{fontenc}
%--------------------------------------

% Literatur
%--------------------------------------
%\RequirePackage[backend=biber,style=alphabetic]{biblatex}
% \addbibresource{literatur.bib}
%--------------------------------------

\RequirePackage[titletoc]{appendix}
\RequirePackage{pdfpages}

% Glossar
%--------------------------------------
\RequirePackage[toc]{glossaries}
\RequirePackage{glossaries-extra}
%\addto\captionsngerman{%
%    \renewcommand*{\glossaryname}{Glossar}%
%}
\makeglossaries
%\setglossarystyle{long3colheader}
\RequirePackage{glossar}

% Font
%--------------------------------------
\RequirePackage{helvet}
%--------------------------------------

% Format
%--------------------------------------
\RequirePackage{float}
%--------------------------------------

% Tikz
%--------------------------------------
\RequirePackage{tikzit}
\input{images/tikzstyle.tikzstyles}
%--------------------------------------

% Tables
%--------------------------------------
\RequirePackage{tabularx}
%--------------------------------------

% To do Notizen
%--------------------------------------
% Wenn die Variable auf customertrue gesetzt wird,
% dann tauchen die To do Notizen nicht im Dokument auf
% \RequirePackage[disable]{todonotes}
\RequirePackage{todonotes}
%--------------------------------------
%
%% Enumeration
%%--------------------------------------
%\RequirePackage{enumitem}
%\setenumerate{noitemsep}
%\setitemize{noitemsep}
%\setlist[2]{noitemsep}
%%--------------------------------------

%% Hyphenation rules
%%--------------------------------------
%\RequirePackage{hyphenat}
%\hyphenation{Mathe-mathik}
%%--------------------------------------

% Math-specific packages
%--------------------------------------
\RequirePackage{amsfonts}
\RequirePackage{amsmath}
\RequirePackage{mathtools}
\RequirePackage{amsthm}
%\setlength{\jot}{13pt}
%
%% Own style for definition
%\newtheoremstyle{definition}
%    {}%         Space above, empty = `usual value'
%    {}%         Space below
%    {}%         Body font
%    {}%         Indent amount (empty = no indent, \parindent = para indent)
%    {\bfseries}% Thm head font
%    {.}%        Punctuation after thm head
%    {\newline}% Space after thm head: \newline = linebreak
%    {}%         Thm head spec
%\theoremstyle{definition}
%\newtheorem{definition}{Definition}
%


%
%% To show an algorithm veeeery niceee
%\usepackage[ruled,vlined]{algorithm2e}
%\SetKwInOut{Boolean}{Boolean}
%
%\RequirePackage{pgfplots}
%%--------------------------------------

% Graphics und Code-Style
%--------------------------------------
\RequirePackage{graphicx}
\RequirePackage{fancyhdr} % need for header images (lhead, chead, rhead)
\RequirePackage{xcolor}
\RequirePackage{colortbl} % Color-Table
\RequirePackage{caption}
\RequirePackage{subcaption}
%--------------------------------------

% Color definitions
%--------------------------------------
\definecolor{optware_green}{HTML}{99CC00}
\definecolor{optware_green_darker}{HTML}{64AA28}
\definecolor{optware_green_brighter}{HTML}{CAFF2A}
\definecolor{optware_blue}{HTML}{003399}
\definecolor{optware_blue_lighter}{HTML}{1995DE}
%--------------------------------------

% Version History
%--------------------------------------
\RequirePackage[tablegrid, tocentry]{vhistory}
%--------------------------------------

% Links, URL, Hyperref
%--------------------------------------
% Um die unschönen roten Ränder der Links
% zu entfernen wird die Option hidelinks
% hinzugefügt
\RequirePackage[hidelinks]{hyperref}
\RequirePackage{cleveref}
%--------------------------------------

%% Special prefs
%%--------------------------------------
%% Unterdrückt die TeX-Warnung von
%% overful or underful hbox
%\hfuzz=5.002pt
%%--------------------------------------

%% TColorBox
%%--------------------------------------
%\usepackage[most]{tcolorbox}
%\tcbset{
%    colframe=optware_blue,
%    highlight math style={
%        enhanced, %<-- needed for the ’remember’ options
%        colframe=red,
%        colback=red!10!white,
%        boxsep=0pt
%    }
%}
%%--------------------------------------


% math shortcuts
%--------------------------------------
\newcommand{\B}{\{0,1\}}
\newcommand{\R}{\mathbb{R}}
\newcommand{\Z}{\mathbb{Z}}
\newcommand{\N}{\mathbb{N}}
%--------------------------------------

\showoutput
